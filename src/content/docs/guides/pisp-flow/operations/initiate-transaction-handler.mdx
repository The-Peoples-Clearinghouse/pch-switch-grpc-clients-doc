---
title: Realizar transferencia PISP
description: Cómo registrar un handler para responder a las solicitudes de transferencia hacia otro participante iniciadas desde una app PISP.
sidebar:
  order: 4
---

import { Tabs, TabItem, Aside } from '@astrojs/starlight/components';

Cuando un Cliente inicia una transferencia hacia un beneficiario en otro participante desde una app PISP, el módulo 3ppi del Switch envía a tu banco una solicitud `InitiateTransaction`. Tu banco debe verificar el `consentToken`, debitar la cuenta del Cliente y ejecutar la transferencia hacia el Switch mediante el método [`ExecuteTransfer`](/pch-switch-grpc-clients-doc/guides/basic-flow/execute-transfer/).

Para recibir estas solicitudes, debes registrar un **handler** en el cliente gRPC. El cliente invocará esta función cada vez que el módulo 3ppi solicite una transferencia a nombre de un Cliente en tu banco.

<Aside type="note">
  Este método es para transferencias **entre participantes distintos**. Para transferencias entre cuentas del mismo participante, consulta [Realizar transferencia PISP interna](/pch-switch-grpc-clients-doc/guides/pisp-flow/operations/initiate-internal-transaction-handler/).
</Aside>

<Aside type="caution">
  Verifica que el `consentToken` sea válido y que la cuenta origen (`accountId`) esté asociada a dicho consentimiento antes de ejecutar el débito. Si el token no es válido, responde con un `ErrorResponse` en lugar de no responder.
</Aside>

## Registrar el handler

<Tabs syncKey="lenguaje">
  <TabItem label="TypeScript">
    ```typescript
    async function initiateTransactionHandler(
      request: ServerInitiateTransactionRequest,
    ): Promise<ServerInitiateTransactionResponse> {
      const partyId          = request.getPartyid();
      const sourceFspId      = request.getSourcefspid();
      const destinationFspId = request.getDestinationfspid();
      const transactionId    = request.getTransactionid();
      const payeeId          = request.getPayeeid();
      const payeeFsp         = request.getPayeefsp();
      const accountId        = request.getAccountid();
      const consentToken     = request.getConsenttoken();
      const geolocation      = request.getGeolocation();
      const deviceId         = request.getDeviceid();
      const sessionId        = request.getSessionid();
      const currencyCode     = request.getCurrencycode();
      const amount           = request.getAmount();
      const note             = request.getNote();
      const threeppienabled  = request.getThreeppienabled();
      const transactionType  = request.getTransactiontype();

      const response = new ServerInitiateTransactionResponse();
      // La fuente de la respuesta es el banco receptor (el nuestro)
      response.setSourcefspid(destinationFspId);
      // El destino de la respuesta es quien originó la solicitud
      response.setDestinationfspid(sourceFspId);
      response.setPartyid(partyId);
      response.setTransactionid(transactionId);

      // Verifica el consent token y que la cuenta esté autorizada
      if (!consentTokenAutorizaCuenta(consentToken, accountId)) {
        const error = new ErrorResponse();
        error.setCode('UNAUTHORIZED');
        error.setMessage('El consent token no autoriza operaciones sobre la cuenta indicada.');
        response.setError(error);
        return response;
      }

      // Verifica la geolocalización
      const geolocalizacionValida = verificarGeolocalizacion(geolocation);

      if (!geolocalizacionValida) {
        const error = new ErrorResponse();
        error.setCode('INVALID_GEOLOCATION');
        error.setMessage('La geolocalización enviada no es válida.');
        response.setError(error);
        return response;
      }

      // Registra la actividad de una operación sensible.
      registrarActividad('INITIATE_TRANSACTION', deviceId, sessionId, geolocation);

      // Ejecuta la transferencia hacia el Switch mediante ExecuteTransfer
      // Ver: /guides/basic-flow/execute-transfer/
      const resultado = await ejecutarTransferencia({
        transactionId,
        payeeId,
        payeeFsp,
        accountId,
        currencyCode,
        amount,
        note,
        transactionType,
      });

      if (!resultado.exitoso) {
        const error = new ErrorResponse();
        error.setCode('TRANSFER_FAILED');
        error.setMessage('No fue posible ejecutar la transferencia.');
        response.setError(error);
        return response;
      }

      response.setTransactionstate(resultado.estado);

      return response;
    }

    // Registrar el handler en el cliente gRPC
    grpcClient.setInitiateTransactionRequestHandlerFn(initiateTransactionHandler);
    ```
  </TabItem>
  <TabItem label="Java">
    ```java
    grpcClient.setInitiateTransactionRequestHandlerFn(req -> {
        String  partyId          = req.getPartyId();
        String  transactionId    = req.getTransactionId();
        String  payeeId          = req.getPayeeId();
        String  payeeFsp         = req.getPayeeFsp();
        String  accountId        = req.getAccountId();
        String  consentToken     = req.getConsentToken();
        String  geolocation      = req.getGeolocation();
        String  deviceId         = req.getDeviceId();
        String  sessionId        = req.getSessionId();
        String  currencyCode     = req.getCurrencyCode();
        String  amount           = req.getAmount();
        String  note             = req.getNote();
        boolean threeppiEnabled  = req.getThreeppiEnabled();
        String  transactionType  = req.getTransactionType();
        // La fuente de la respuesta es el banco receptor (el nuestro)
        String  sourceFspId      = req.getDestinationFspId();
        // El destino de la respuesta es quien originó la solicitud
        String  destinationFspId = req.getSourceFspId();

        ServerInitiateTransactionResponse.Builder response =
                ServerInitiateTransactionResponse.newBuilder()
                        .setSourceFspId(sourceFspId)
                        .setDestinationFspId(destinationFspId)
                        .setPartyId(partyId)
                        .setTransactionId(transactionId);

        // Verifica el consent token y que la cuenta esté autorizada
        if (!consentTokenAutorizaCuenta(consentToken, accountId)) {
            return response
                    .setError(ErrorResponse.newBuilder()
                            .setCode("UNAUTHORIZED")
                            .setMessage("El consent token no autoriza operaciones sobre la cuenta indicada.")
                            .build())
                    .build();
        }

        // Verifica la geolocalización
        if (!verificarGeolocalizacion(geolocation)) {
            return response
              .setError(ErrorResponse.newBuilder()
                .setCode("INVALID_GEOLOCATION")
                .setMessage("La geolocalización enviada no es válida.")
                .build())
              .build();
        }

        // Ejecuta la transferencia hacia el Switch mediante ExecuteTransfer
        // Ver: /guides/basic-flow/execute-transfer/
        ResultadoTransferencia resultado = ejecutarTransferencia(
                transactionId, payeeId, payeeFsp, accountId, currencyCode, amount, note, transactionType);

        if (!resultado.exitoso) {
            return response
                    .setError(ErrorResponse.newBuilder()
                            .setCode("TRANSFER_FAILED")
                            .setMessage("No fue posible ejecutar la transferencia.")
                            .build())
                    .build();
        }

        // Registra la actividad de una operación sensible.
        registrarActividad('TRANSACTION', deviceId, sessionId, geolocation);

        return response
                .setTransactionState(resultado.estado)
                .build();
    });
    ```
  </TabItem>
  <TabItem label="C#">
    ```csharp
    InitiateTransactionRequestHandlerDelegate initiateTransactionHandler =
        async (ServerInitiateTransactionRequest request) =>
    {
        var partyId          = request.PartyId;
        var sourceFspId      = request.SourceFspId;
        var destinationFspId = request.DestinationFspId;
        var transactionId    = request.TransactionId;
        var payeeId          = request.PayeeId;
        var payeeFsp         = request.PayeeFsp;
        var accountId        = request.AccountId;
        var consentToken     = request.ConsentToken;
        var geolocation      = request.Geolocation;
        var deviceId         = request.DeviceId();
        var sessionId        = request.SessionId();
        var currencyCode     = request.CurrencyCode;
        var amount           = request.Amount;
        var note             = request.Note;
        var transactionType  = request.TransactionType;

        var response = new ServerInitiateTransactionResponse
        {
            // La fuente de la respuesta es el banco receptor (el nuestro)
            SourceFspId      = destinationFspId,
            // El destino de la respuesta es quien originó la solicitud
            DestinationFspId = sourceFspId,
            PartyId          = partyId,
            TransactionId    = transactionId,
        };

        // Verifica el consent token y que la cuenta esté autorizada
        if (!ConsentTokenAutorizaCuenta(consentToken, accountId))
        {
            response.Error = new ErrorResponse
            {
                Code    = "UNAUTHORIZED",
                Message = "El consent token no autoriza operaciones sobre la cuenta indicada.",
            };
            return response;
        }

        // Verifica la geolocalización
        if (!VerificarGeolocalizacion(geolocation))
        {
          response.Error = new ErrorResponse
          {
            Code    = "INVALID_GEOLOCATION",
            Message = "La geolocalización enviada no es válida.",
          };
          return response;
        }

        // Ejecuta la transferencia hacia el Switch mediante ExecuteTransfer
        // Ver: /guides/basic-flow/execute-transfer/
        var resultado = EjecutarTransferencia(transactionId, payeeId, payeeFsp,
                                              accountId, currencyCode, amount, note, transactionType);

        if (!resultado.Exitoso)
        {
            response.Error = new ErrorResponse
            {
                Code    = "TRANSFER_FAILED",
                Message = "No fue posible ejecutar la transferencia.",
            };
            return response;
        }

        // Registra la actividad de una operación sensible.
        registrarActividad('TRANSACTION', deviceId, sessionId, geolocation);

        response.TransactionState = resultado.Estado;

        return response;
    };

    // Registrar el handler en el cliente gRPC
    grpcClient.setInitiateTransactionRequestHandler(initiateTransactionHandler);
    ```
  </TabItem>
</Tabs>

## Campos del mensaje entrante (`ServerInitiateTransactionRequest`)

| Campo | Tipo | Descripción |
|---|---|---|
| `partyId` | `string` | Identificador del Cliente pagador (ej. número de celular). |
| `sourceFspId` | `string` | FSP que origina la solicitud (módulo 3ppi del Switch). |
| `destinationFspId` | `string` | FSP al que va dirigida la solicitud (tu banco). |
| `transactionId` | `string` | Identificador único de la transacción generado por el Switch. |
| `payeeId` | `string` | Identificador del beneficiario. |
| `payeeFsp` | `string` | FSP del beneficiario. |
| `accountId` | `string` | Identificador de la cuenta origen del Cliente. |
| `consentToken` | `string` | Token de consentimiento del Cliente. |
| `geolocation` | `string` | Geolocalización asociada a la solicitud. |
| `deviceId` | `string` | Identificador del dispositivo del Cliente. |
| `sessionId` | `string` | Identificador de la sesión de la app PISP. |
| `currencyCode` | `string` | Moneda de la transferencia (ej. `'MXN'`). |
| `amount` | `string` | Monto a transferir. |
| `note` | `string` | Concepto o nota de la transferencia. |
| `threeppienabled` | `boolean` | Indica si la transacción fue iniciada mediante PISP. |
| `transactionType` | `string` | Tipo de transacción. |

Ver referencia completa: [ServerInitiateTransactionRequest](/pch-switch-grpc-clients-doc/reference/server-initiate-transaction-request/)

## Respuesta (`ServerInitiateTransactionResponse`)

| Campo | Tipo | Requerido | Descripción |
|---|---|---|---|
| `sourceFspId` | `string` | Sí | FSP que responde (tu banco). Usa el `destinationFspId` de la solicitud. |
| `destinationFspId` | `string` | Sí | FSP destino de la respuesta. Usa el `sourceFspId` de la solicitud. |
| `partyId` | `string` | Sí | Identificador del Cliente (mismo que el recibido). |
| `transactionId` | `string` | Sí | Identificador de la transacción (mismo que el recibido). |
| `transactionState` | `string` | Sí | Estado final de la transacción. |
| `error` | `ErrorResponse` | No | Incluir si no fue posible ejecutar la transferencia. |

Ver referencia completa: [ServerInitiateTransactionResponse](/pch-switch-grpc-clients-doc/reference/server-initiate-transaction-response/)

## Cuando la transferencia falla

Si el `consentToken` no es válido, la cuenta no tiene fondos suficientes, o el Switch rechaza la transferencia, incluye un `ErrorResponse` en la respuesta y omite el campo `transactionState`.
